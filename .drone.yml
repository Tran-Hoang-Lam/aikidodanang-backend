pipeline:
  build:
    image: java:openjdk-8
    commands:
      - chmod +x gradlew
      - ./gradlew clean build
  docker:
    image: plugins/docker
    debug: true
    secrets: [ docker_username, docker_password ]
    tags: lamth2/aikidodanang-backend

